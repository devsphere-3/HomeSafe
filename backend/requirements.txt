# HomeSafe Backend — Python dependencies
# Install: pip install -r requirements.txt

# Core ML
opencv-python-headless==4.10.0.84
numpy==1.26.4
mediapipe==0.10.14

# API server
fastapi==0.115.5
uvicorn[standard]==0.32.1
python-multipart==0.0.12

# GPIO (Raspberry Pi only — skip on dev machine)
# RPi.GPIO==0.7.1
